"""Legacy search tests for removed pre-normalization search module."""

import pytest

pytest.skip(
    "Legacy SearchQuery/ImageTag tests are obsolete after tag normalization.",
    allow_module_level=True,
)
